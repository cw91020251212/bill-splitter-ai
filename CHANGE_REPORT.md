# 分帳規則及數量核對變更報告

## 變更日期

2026-09-09

## 變更原因

現有版本已支援兩種認領方式：只填數字代表份數比例，數字加單位代表實際數量。然而，當單據有實際總量（例如 10 杯飲品）而所有認領人合共只填 7 杯時，系統需要明確指出尚欠 3 杯，避免未認領金額無聲無息地消失或由錯誤的人承擔。

## 實作內容

保留原有雙模式分攤方式。只填數字的回覆繼續按份數比例分攤；數字加單位的回覆在單據總量可知時按實際單位價值分攤。當同一項目全部採用實際單位而認領總量少於單據總量，步驟三會顯示尚欠數量及估算金額，並要求使用者選擇由墊支人、已認領人士平均、或指定人士承擔。當認領總量超過單據總量，系統會阻止產生報告並要求返回修改。

對於資料不足或同一項目混合使用實際單位與份數比例的情況，系統保留現有彈性：資料不足時按份數比例處理；混合輸入時先計算實際單位，再將剩餘金額按份數比例分配。

詳細結果新增未認領數量承擔明細，列出數量、單位、金額及承擔原因，讓最終報告可供群組核對。

## 驗證

已通過 Babel JSX 轉換、JavaScript 語法解析、service worker 語法檢查及 `git diff --check`。本報告與程式修正會隨同新版一併部署。
